{
  "name": "llm-harness-bench",
  "version": "0.1.0",
  "description": "Benchmark LLM capability with and without a harness (tools + output schema + structured prompts) against the webserver task environment.",
  "type": "module",
  "main": "src/cli.js",
  "bin": {
    "llm-harness-bench": "src/cli.js"
  },
  "scripts": {
    "serve": "node src/cli.js serve",
    "bench": "node src/cli.js",
    "one": "node src/bench.js",
    "aggregate": "node src/aggregate.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "private": true
}
